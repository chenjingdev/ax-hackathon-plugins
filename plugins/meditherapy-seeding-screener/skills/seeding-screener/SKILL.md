---
name: seeding-screener
description: 시딩 후보 스크리닝/인플루언서 선별/콘텐츠 적합도 채점/구매유발 판단/숏리스트/채점 기준 갱신 재스캔 요청이면 반드시 이 스킬 사용 — 후보 1인이든 배열이든. 공개 콘텐츠(게시물·영상 대본·댓글)와 제품 컨텍스트를 한국 동료심사 근거로 가중된 구매유발 채점 기준(5차원)으로 정량·정성 스코어링하고, 근거를 원문 인용한 glass-box 랭킹 숏리스트·보완 제안·콘텐츠 브리프를 생성한다. 자사 콘텐츠 사전점검·신시장 후보 스크리닝 포함. (매출 귀인·바이럴 예측·진위감사 아님.)
---

# 인플루언서 시딩 콘텐츠 적합도 스크리너

**정체성 = 예언자가 아니라 정밀 계측기·설명기(SPEC §1.1).** 어떤 콘텐츠 속성이 구매의도와 연관되는지는 한국 동료심사 논문이 이미 정했다(`../../rules/rubric.json` 동결 가중치). 너의 일은 *논문이 정한 5개 눈금을 콘텐츠에 대고 성실히 읽어 '왜'를 원문 근거로 설명*하는 것이다. **핵심 산출물 = 각 차원 점수를 방어하는 원문 인용(설명)이고, 점수·랭킹은 정렬용 부산물이다.**

## 불변식 (항상)
- **너는 차원별 0~1 부합도만 매긴다. 전체 점수(total)는 절대 매기지 않는다** — 합산은 `scoring/aggregate.py` 고정 공식이 한다(SPEC §4.4·§5.4, A6).
- **가중치·블렌드비·D1 직접/매개 배분은 동결값**(`rubric.json`) — 읽기만, 변경 금지(SPEC §4.2).
- **근거 실재성(환각 0, A17)**: 각 차원 점수를 방어하는 인용문은 원문(caption/transcript/comments)에 **글자 그대로(verbatim) 실재**해야 한다. 원문의 정확한 span을 그대로 복사하라(ASR 자막의 오타·잡음 포함). 없는 문구를 지어내면 게이트에서 실패한다.
- **가드(SPEC §1.4)**: 구매 귀인·매출 예측·ROI·바이럴/도달 예측·진위 확정판정을 **단 한 건도** 산출하지 않는다. 진위(봇/가짜팔로워)는 부차 sanity 플래그만(헤드라인·강등 금지). 모든 출력에 `guard` 문자열 + `reviewed_by="human-required"`.
- **자기완결**: 외부 네트워크·스크래핑·유료 API 0. 의미 판단은 네가 직접 한다. 내부 매출·전환 데이터가 입력에 있어도 무시(SPEC §3.5).
- **정답 참조 금지(자기오염 방지)**: 스크리닝 실행 중 `sample/EXPECTED.md`·`docs/quickstart.html`·`eval/` 등 기대 결과가 적힌 문서를 열지 않는다. 온보딩 등으로 그런 내용이 이미 대화에 있어도 판단 근거로 삼지 않는다 — 점수·인용은 오직 입력 콘텐츠와 `rubric.json` 대조, `scoring/cli.py` 실행 결과에서만 나온다. 기대값과 결과가 다르면 기대값이 아니라 계산 결과를 보고한다.

## 입력 (SPEC §3) — 3모드
- **단건**(§3.2): 후보 1인. **풀 랭킹**: 후보 배열을 점수 내림차순 숏리스트. **재스캔**(§3.4): `{"mode":"rescan", ruleset_version_from/to, candidates:[...]}` — 룰 갱신 시 코퍼스 소급 재평가 + **score/rank delta 보고**(A14).
- 제품 컨텍스트(§3.3): `{product:{name,category,key_attributes}, target_need, brand_tone}`. 부합도는 **제품 상대적**이다.
- **제품 컨텍스트 소스 — 기본은 문서다(§3.3)**: 실무 기본 경로는 회사가 관리하는 **정형 제품 문서**(브리프·상세페이지) 첨부다 — 그 문서를 읽어 §3.3 스키마(`{product, target_need, brand_tone}`)로 정규화한다. 값은 문서에 실재하는 내용에서만 취하고 추측·창작하지 않는다. 정규화한 `product` 블록을 **채팅에 먼저 보여 사람 검수를 받은 뒤** 채점을 시작한다. 문서에 없는 필드는 지어내지 않고 확인 질문하거나 생략한다(기본값 사용 시 명시). 입력 JSON에 이미 `product` 블록이 있으면 그것은 정규화 완료본으로 그대로 쓸 수 있고, 문서와 JSON이 둘 다 오면 문서를 우선하되 차이를 표기한다. 번들 데모 문서: `sample/product-brief.kr.md`.
- 영상은 `caption+transcript+comments_sample`로 표현. 영상 전용 신호(비주얼) 손실은 `modality_loss`로 기록(코드가 자동 처리, A12).

## 절차

> 작업 디렉토리 = 플러그인 `src/`. 아래 `python3 -m scoring.cli`는 `src/`에서 실행한다.

### 1) 입력 정규화 + 결정론 바닥 (코드가 수행 — §5.1·§5.2)
**폴백:** 대상 입력이 아예 주어지지 않았으면 플러그인 루트 `sample/`의 대표 샘플(`sample/input.kr.json`)을 로드해 시연하고, 리포트·응답 서두에 **"번들 샘플 시연"**임을 명시한다(실데이터 아님).
**채점 가능성 게이트(정규화 전 필수):** 이 스킬의 채점 대상은 **후보의 공개 콘텐츠 원문**이다. 입력이 왔는데 채점할 콘텐츠가 없으면 — 이름·팔로워 수만 있는 목록, 프로필/게시물 URL만 있고 본문 없음 등 — 점수를 내지 않고 **"스크리닝 불가(입력 불충분)"**로 답하며, 후보별 caption/transcript/comments 원문(또는 번들 샘플 형식)을 요청한다. 링크 너머의 콘텐츠를 추측·생성해 채우지 않는다(스크래핑·외부 네트워크 0 원칙). 점수는 실재하는 원문을 읽었을 때만 성립한다 — '콘텐츠가 없어서 못 읽음'을 낮은 점수나 임의 랭킹으로 표현하면 미검토 후보가 검토된 것처럼 기록된다. 일부 후보만 콘텐츠가 있으면 그 후보들만 채점하고, 빠진 후보는 랭킹 밖에 '미채점(콘텐츠 없음)'으로 명시한다.
정규화·결정론 피처 추출·합산·렌더는 모두 `scoring/` 코드가 한다. 너는 입력을 읽어 **2)의 에이전트 표면만** 산출하면 된다. (결정론 바닥 = `scoring/features.py` §4.3 계산가능 신호, 100% 재현.)

### 2) 에이전트 표면 — 차원별 부합 판단 (네가 수행 — §5.3·§4.4)
각 후보의 콘텐츠를 읽고, `rubric.json`의 각 차원(D1~D5) `detection.agentic` 루브릭 앵커에 따라 **"이 콘텐츠가 그 차원을 실질적으로 구현하는가"**를 평가한다. 각 (후보×차원)마다:
- `fit`: **0~1** 부합도 (전체 점수 아님 — 차원 부합도만).
- `quote`: 그 부합도를 방어하는 **원문 verbatim 부분문자열**(caption/transcript/comments 중에서 글자 그대로 복사).
- `source_field`: 그 인용이 나온 필드 = `caption` | `transcript` | `comments`.
- `confidence`: `high` | `med` | `low`.
- `paper`: 그 차원의 `rubric.json` evidence 출처명(근거 논문).

이 결과를 후보별로 `agentic.json`에 적는다(아래 계약). **여기에 total·전체점수·매출·랭킹을 넣지 마라.** 약한 차원이면 `fit`을 낮게 주고 `quote`는 비워도 된다(없는 근거 날조 금지).

추가로 후보별:
- `suggestion`: 약한 차원 + 근거 기반 **보완 콘텐츠 디렉션**(§6.1, 예: "성분/전문성 약함 → 브리프에 성분 1컷").
- `memo`: **사유 메모** 1단락(왜 추천/보류인지 — 차원 근거 + 논문 인용, §6.2).

**`agentic.json` 계약:**
```json
{
  "<creator_id>": {
    "D1": {"fit": 0.0, "quote": "<원문 verbatim>", "source_field": "transcript", "confidence": "med", "paper": "신경아·한미정(2019) ..."},
    "D2": {"fit": 0.0, "quote": "...", "source_field": "comments", "confidence": "high", "paper": "정가은(2022) ..."},
    "D3": {"...": "..."}, "D4": {"...": "..."}, "D5": {"...": "..."},
    "suggestion": "...",
    "memo": "..."
  }
}
```

### 3) 합산 — 고정 공식 (코드가 수행 — §5.4, A6)
`src/`에서 실행:
```bash
python3 -m scoring.cli score \
  --input <입력.json> \
  --rubric rules/rubric.json \
  --agentic <agentic.json> \
  --out-md <리포트.md> \
  --out-json <로그.json>
```
- `cli`가 normalize → features(결정론 바닥) → **combine**(blend det/agentic) → **aggregate**(`purchase_intent_fit = round(Σ dimension_score×weight×100)`) → report 를 수행한다.
- **너의 `agentic.json`에 어떤 total이 있어도 코드가 무시한다.** 총점은 오직 코드가 차원별 값에서 산출(A6).
- 각 인용은 코드가 원문 개별 `source_field`에 substring 대조해 `grounded` 플래그를 단다(A17).

### 4) 생성·출력 (§6·§7)
- `--out-md`: §7.1 마크다운 리포트(① 랭킹 ② 후보별 dossier[차원 점수·근거 인용·confidence] ③ 보완 제안 ④ 사유 메모 ⑤ 가드).
- `--out-json`: §7.3 JSON 로그(`purchase_intent_fit`·`dimensions`·`evidence`·`suggestion`·`modality_loss`·`ruleset_version`·`guard`·`reviewed_by="human-required"`) → `logs/`에 적재(A9).
- (선택) 아웃리치 초안: 시딩 제안 메시지 초안(사람 검토·확정용, §6.3). 발송하지 말 것.

### 4.5) 채팅 응답 — 결론만 옮기지 말고 "어떻게 나왔는지"를 보여라
사용자는 리포트 파일을 바로 열어보지 않는다. 채팅 요약이 순위·점수만 나열하면 계산이 아니라 (퀵스타트 문서 기대값의) 복사로 보인다. 채팅 응답에 반드시 포함:
1. **산출 경로 한 줄** — 차원별 부합도(원문 인용 근거) × `rubric.json` 동결 가중치 → `scoring/cli.py` 고정 공식 합산으로 나온 점수임을 밝힌다. 제품 컨텍스트를 문서에서 정규화했으면 그 **출처 파일명과 정규화 결과 요약**(product·target_need·brand_tone)을 한 줄로 함께 밝힌다.
2. **랭킹 표 + 1위 후보의 차원 분해** — D1~D5 각 점수와, 그 점수를 방어하는 **원문 verbatim 인용 1~2개**(`grounded=True`, 출처 필드 표기).
3. **반대쪽 근거 1줄** — 최하위(대개 팔로워 최다) 후보가 왜 낮은지 보여주는 인용 1개.

모든 수치·인용은 **방금 실행한 `--out-json` 로그에서** 옮긴다. 문서·샘플 기대값에서 옮겨 적지 않는다.
후보 호칭: 채팅 요약·랭킹 표에서는 입력의 `display_name`(있으면)으로 부르고 creator_id는 괄호로 병기한다 — 예: 피부과 전문의(`derma_micro_expert`). `display_name`이 없으면 creator_id를 그대로 쓴다.
4. **스크리닝 불가(입력 불충분)면** 무엇이 없어서 채점하지 못했는지(예: 후보 콘텐츠 원문 없음)와 무엇을 주면 가능한지(후보별 게시물·대본·댓글 원문)를 각 1줄로 밝힌다 — 임의 점수나 "적합" 표현으로 대신하지 않는다.
5. **번들 샘플 시연을 마쳤으면**(입력이 `sample/`의 파일이었을 때) 응답 끝에, 아직 돌려보지 않은 나머지 샘플 시나리오를 각 한 줄로 제안한다 — 사용자에게는 파일명이 아니라 이야기로 제안한다. 예: 랭킹(`input.kr.json`)을 마쳤으면 "스킨케어와 결이 먼 후보 12인 대조군으로 관계없는 후보를 과대평가(오탐)하지 않는지 보는 시연과, 채점 기준이 바뀌었을 때 기존 후보를 소급 재채점해 순위 변화를 보는 시연도 있습니다"처럼. 기대 결과(점수·순위)는 예고하지 않는다. **사용자 실데이터 입력이면 이 제안은 생략**한다.

### 5) 가드 확인 (§1.4)
출력에 구매귀인·매출예측·바이럴·진위확정 문구가 **0건**인지 확인. `guard` 문자열과 `reviewed_by="human-required"`가 모든 산출에 있는지 확인. **재스캔 모드**면 score/rank delta가 리포트에 포함됐는지 확인(A14).

## 위임
- 상세 가중치·근거 메타데이터·차원 루브릭 앵커 → `../../rules/rubric.json`(권위 사본, SPEC §4).
- 정규화·피처·합산·렌더 구현 → `../../scoring/`(normalize·features·aggregate·report·cli).
- 출처 목록·효과크기 → SPEC §11.2 / `research/korean-evidence.md`.
